/**
 * Métadonnées de requête réutilisées par les services pour tracer les connexions.
 *
 * @author SINGO Yao Dieu Donnée
 * @since 0.0.1
 * @public
 */
export type RequestMeta = {
  ipAddress: string;
  userAgent: string;
};
